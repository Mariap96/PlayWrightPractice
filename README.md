# PlayWrightPractice


### Ejecución por linea de comando

> npx playwright test --grep "nombre test" --project="chromium" --headed

-- Headed nos permite abrir el browser 

-- project nos permite indicar por medio de que browser 

Comando para correr un setup
> npx playwright test --grep "Get all the usernames registered" --project="admin" --headed
